const fs = require("fs"); let lines = fs.readFileSync("src/pages/admin/AdminDashboard.jsx", "utf8").split("\n"); lines[41] = "            ⚠️ {overdue.length} pago{overdue.length > 1 ? `s` : ``} vencido{overdue.length > 1 ? `s` : ``}"; lines[46] = "            ⏳ {dueSoon.length} pago{dueSoon.length > 1 ? `s` : ``} vence en menos de 3 dias"; lines[126] = "          <p className=\"text-muted\">Barba Construction - Resumen operativo</p>"; lines[167] = "                      PRJ-{String(p.project_number).padStart(4,`0`)} - {p.title}"; fs.writeFileSync("src/pages/admin/AdminDashboard.jsx", lines.join("\n"), "utf8");
